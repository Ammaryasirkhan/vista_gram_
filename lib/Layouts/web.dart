import 'package:flutter/material.dart';

class WebS extends StatelessWidget {
  const WebS({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
          child: Column(
        children: [Text("Ammar web")],
      )),
    );
  }
}
